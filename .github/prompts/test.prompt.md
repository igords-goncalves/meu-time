Your goal is to generate a new simple component to test the prompt file.

Follow the instructions.

Requirements for the component:

- Use `<div>` for all stuff;
- It has to be at least one `<p>` tag;
